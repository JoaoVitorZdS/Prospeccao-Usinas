# js/supabase-config.js agora é committado (não está mais no .gitignore),
# então isto não precisa mais forçar sua inclusão — mantido só pelas
# exclusões abaixo, caso alguém publique via `vercel deploy` direto do disco
# em vez de deploy pelo GitHub.

.DS_Store
*.log
node_modules/
